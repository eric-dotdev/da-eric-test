{
  "name": "data-connect-learning",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web-app dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
